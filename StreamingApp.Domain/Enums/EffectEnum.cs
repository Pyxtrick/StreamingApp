namespace StreamingApp.Domain.Enums;

// TODO: implement funny effect
public enum EffectEnum
{
    none,
    // rotates the message on its head
    flip,
    // add rainbow collor to all letters or word
    rainbow,
    // reverts the text "txet eht strever"
    revert,
    // bounces the words
    bounce,

    // https://modrinth.com/mod/text-animator
    // shakes each letter
    shake,
    // word wave
    wave,
    // wiggle each letter
    wiggle,
    // randomices the words
    randamise,
    // translates the message with translate a lot of times
    translatehell,
    // Gigantify / enlarge an Emote (twitch)
    gigantify,
}
